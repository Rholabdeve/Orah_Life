import 'package:flutter/material.dart';

class PerceptionOrder extends StatefulWidget {
  const PerceptionOrder({super.key});

  @override
  State<PerceptionOrder> createState() => PerceptionStateOrder();
}

class PerceptionStateOrder extends State<PerceptionOrder> {
  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Column(
          children: [],
        ),
      ),
    );
  }
}
